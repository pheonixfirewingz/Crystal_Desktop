[package]
name = "libcrystalmatrix"
version.workspace = true
authors.workspace = true
edition.workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
libprotocol = { path = "../LibProtocal"}
lazy_static = "1.5.0"

